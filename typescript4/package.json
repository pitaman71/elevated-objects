{
  "name": "@pitaman71/omniglot-introspect",
  "version": "0.8.9",
  "description": "Framework for specifying introspect-able object types that can accurately initialize themselves, marshal themselves to/from JSON and other formats with little to no additional code.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "start": "tsc -w",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alan Pita",
  "license": "ISC",
  "devDependencies": {
    "@types/luxon": "^2.3.0",
    "@types/node": "^14.14.31",
    "typescript": "^4.3.5"
  },
  "peerDependencies": {
    "@pitaman71/omniglot-code-instruments": "^0.8.1",
    "crypto-random-string": "^3.3.1",
    "moment": "^2.29.1"
  }
}
